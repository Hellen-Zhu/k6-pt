# 容量测试计划 — 真实业务倍数(1x/2x/5x/10x)验证

> 目的:回答"系统能承受多少倍峰值业务量"。产出不是某个 req/s 数字,而是一个业务倍数
> **Nx** 结论 + 拐点曲线 + 达标判定,管理层可直接拍板。
>
> 状态:**待锚定 1x**(见第 0 步,两个 TBD 填上后本计划即可执行)。
> 动因:2026-08-17 SIT ladder 轮(`trade-mix-full_sit_mix-ladder_20260817-024455`)暴露的
> 三个有效性问题,见附录 A。

---

## 第 0 步:从生产数据推导 1x(唯一的外部输入,先做)

所有服务都在单一 gateway 之后,以 **最近 4~8 周生产 gateway 访问日志** 为一手数据源:

1. 按 endpoint 统计 **最忙 1 小时** 的请求量(峰值小时,不是日均 —— FX 流量集中在
   开盘/收盘/fixing 窗口,日均会低估 3~5 倍);
2. 推导两个数:

```
1x 总速率 R      = 峰值小时总请求数 ÷ 3600            (HTTP req/s)
各 endpoint 份额 = 该 endpoint 峰值小时请求数 ÷ 总数   → 替换 trade-mix-full.js 的 MIX 表
```

拿不到网关日志时的退路:业务量倒推(日创建/修改/事件笔数 × 峰时集中系数;查询量 =
活跃用户 × blotter 刷新频率)。倒推是二手估计,报告必须注明来源。

**本步产出(填入即解锁全计划):**

| 项 | 值 | 来源 |
|---|---|---|
| `R`(1x 总速率) | **TBD** req/s | gateway 峰值小时日志 |
| `MIX` 真实配比 | **TBD**(替换 `src/mixed/trade-mix-full.js:69` 的 placeholder) | 同上 |

---

## 第 1 步:清掉三个有效性地雷(不清掉,测的就不是系统能力)

| # | 地雷 | 现象(0817 轮) | 对策 |
|---|---|---|---|
| 1 | **blotter 自灌水**:查询按 `dealDate = CURRENT_DATE` 过滤,mix 造的交易全是当天的,跑得越久每次查询扫的行越多 | blotter p50 从 ~5s 单调爬到 10.4s,p99 21.5s;系统退化与数据灌大混在一起,不可归因 | 每轮开跑前将 SIT 库恢复到 **生产量级固定快照**(或轮间清理当日 mix 数据),让 DB 状态成为受控变量。快照下 1x 仍秒级 → 真缺陷,走 bug-reporting 提单 |
| 2 | **amend-cycle 池毒化**:退化后 reject 回程失败,id 卡 pending,轮换回访持续 409(脚本注释已记载的已知模式) | 6959 个 technical 失败,归因全部为 http-409 | 每轮前重新 seed `amend-cycle-ids`;"池健康检查"写进开跑 checklist |
| 3 | **发压端 VU 池未随速率换算**:阶梯扩到 ~150 req/s 但沿用 40/150 的池 | 顶层 step 即使系统健康也必然丢迭代,dropped 信号被污染 | 按下表公式每档换算(推导见 0818 会话,基于 SLA p95/p99 信封 + splitByRatio 按 ratio 切池) |

**VU 池换算公式**(R = 该轮目标总 req/s;当前 MIX 与 SLA 下成立,两者变了要重推):

```
preAllocatedVUs ≈ 0.87 × R     (SLA-p95 健康水位 ×1.2,瓶颈 flow 归一化)
maxVUs          ≈ 2.2  × R     (SLA-p99 信封   ×1.2,瓶颈 flow = event-chain)
```

语义:预分配覆盖全程健康水位(零运行时扩容开销);maxVUs 封在 p99 信封 ——
**dropped/s > 0 ⟺ 被测系统延迟越过 p99 SLA 信封**,拐点信号有明确定义。

---

## 第 2 步:执行矩阵(按序执行,一轮只改一个变量)

| 轮次 | 目的 | 命令(sit 为例) | 判定 |
|---|---|---|---|
| ① smoke | 链路通、脚本对 | `./run.sh trade-mix-full sit smoke` | 全绿即过 |
| ② mix-ref | 定基线 | `./run.sh trade-mix-full sit mix-ref` ×3 | 3 轮稳定 PASS 后 promote 到 `baselines/` |
| ③ **1x 负载** | SLA 达标验证 | `./run.sh trade-mix-full sit mix RATE=R DURATION=30m` | **必须 PASS**(SLA p95/p99 + 业务成功率 >0.99) |
| ④ **2x 余量** | 增长/容灾余量 | 同上,`RATE=2R` | **必须 PASS** —— 容量红线的行业惯例 |
| ⑤ 5x/10x 阶梯 | 找拐点 | 重建 `mix-ladder` 阶梯为 1x→2x→3x→5x→7x→10x(literal,需改 profile 文件;VU 池按 10x 档换算) | 测量轮,不判 PASS/FAIL;交付物 = 拐点 **Nx** |
| ⑥ soak | 稳定性 | soak profile,1x 跑 4~8h(一个交易日) | 延迟/内存无漂移 |
| ⑦ spike(后续) | 行情事件突刺 | spike profile(NFP/央行公告形态) | 冲击后恢复时间 |

**执行纪律**(来自 performance-testing skill 的方法论,重申以免走样):

- ③④判定轮 **各跑两遍**,不可复现的结果按噪音处理;
- 轮与轮之间只允许改"速率"这一个变量(代码版本、基础设施、数据快照全部冻结);
- ⑤中某档 dropped/s 抬头即为拐点,其后档位数据仅用于观察退化形态(优雅降级还是雪崩);
- verdict 权威是 summary(三类错误分拆 + 阈值),不是 dashboard —— 本系统业务失败也返回 200。

**方法论提醒 —— 两类轮次回答两个不同的问题,判定逻辑必须分开:**

- ③④是**合规题**:"满足 SLA 吗?" → 二元 PASS/FAIL,阈值即 verdict;
- ⑤是**测绘题**:"拐点在哪?" → 没有 PASS/FAIL,交付物是曲线和 Nx 数字。
  拿测绘轮的成功率去下合规式结论(如 0817 轮的"92.3%")是答非所问。

---

## 第 3 步:最终报告模板

```markdown
# 容量测试报告:FX 结构化产品交易系统 — <日期>
**问题**:系统能承受多少倍峰值业务量?
**结论**:1x/2x PASS|FAIL;拐点 = Nx(瓶颈:<证据指向>);10x 退化形态 = <描述>
**环境**:SIT <规格>,数据快照 <版本/量级>,构建 <版本>

| 档位 | 目标 req/s | 实际 req/s | p50 | p95 | p99 | 业务失败% | 技术失败% | dropped/s |
|---|---|---|---|---|---|---|---|---|

**拐点**:<首个 dropped/s>0 的档位及对应延迟形态>
**瓶颈假设**:<CPU/DB/锁/N+1,附证据>
**前提与保留**(honesty notes,脚本注释里现成有,照搬):
- SIT 与生产规格差异 → 结论为下界
- 时间线压缩:approve 紧跟 create(生产为分钟级)、事件打在秒龄交易上(生产为天级)
- checker 份额落地为 approve .10 + reject .20(生产以 approve 为主,同状态机写路径,容量等价)
```

回归基线红线的差异走 bug-reporting 提单;例行轮次进 CI extended tier(见
regression-ci-strategy skill)。

---

## 附录 A:0817 ladder 轮的三个教训(本计划的动因)

1. 阶梯扩了 ~3.75 倍但 VU 池没跟着扩 → 顶层 step 的 dropped 信号是发压端造成的,
   不能计入系统能力(80 req/s 及以下各档信号有效);
2. blotter 延迟爬坡与当日数据灌大混杂,不可归因 → 引出快照纪律;
3. 用测绘轮下了合规式结论 → 引出两类轮次的判定分离。

复核用数字:该轮 dropped/s ~50 的平台,与"分池上限 ÷ 实测退化时长"逐 flow 加总
(query ~34/s + amend ~7/s + 其余 ~5-8/s)精确吻合 —— 模型自洽,可放心用于池 sizing。
